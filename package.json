{
  "name": "mental-health-platform",
  "version": "1.0.0",
  "description": "Mental Health Assessment Platform",
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_ENV=development node Server/src/server.js",
    "build": "vite build && cd Server && npm run build",
    "start": "cd Server && npm start",
    "install-all": "npm install && cd Server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.1",
    "autoprefixer": "^10.4.0",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "gsap": "^3.14.2"
  }
}
